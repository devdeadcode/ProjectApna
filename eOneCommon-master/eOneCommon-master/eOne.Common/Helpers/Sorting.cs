using System;
using System.Collections.Generic;

namespace eOne.Common.Helpers
{
    class Sorting
    {

        public IEnumerable<object> SortRecords(IEnumerable<object> records, IEnumerable<Tuple<string, bool>> fields, int numberOfRecords)
        {
            //todo
            return records;
        }
        public IEnumerable<object> SortRecords(IEnumerable<object> records, IEnumerable<Tuple<string, bool>> fields)
        {
            //todo
            return records;
        }
        public IEnumerable<object> SortRecords(IEnumerable<object> records, string fieldName, bool asc = true)
        {
            //todo
            return records;
        }

    }
}
